version: '3.1'

services:
  party-server:
    image: featurehub/party-server:1.5.9-RC
    restart: always
    user: 999:999
    volumes:
      - featurehub-h2-data:/db
      - ./app-config:/etc/app-config
    ports:
      - 8085:8085
volumes:
  featurehub-h2-data:
