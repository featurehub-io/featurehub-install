version: '3.1'

services:
  party-server:
    image: featurehub/party-server:latest
    restart: always
    volumes:
      - featurehub-h2-data:/db
    ports:
      - 8085:80
volumes:
  featurehub-h2-data:
